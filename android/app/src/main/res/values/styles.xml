<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:datePickerDialogTheme">@style/Dialog.Theme</item>
        <item name="android:timePickerDialogTheme">@style/Dialog.Theme</item>
    </style>

    <!-- Configuration for Dialog.theme. -->
    <style name="Dialog.Theme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">#ADC178</item>
        <!-- BackgroundColor of the date modal -->
        <item name="colorAccent">#486F21</item>
<!--        <item name="android:colorControlActivated">#FFFFFF</item>-->
        <!-- Change textColor of the date modal -->
        <item name="android:textColorPrimary">#2C2C2C</item>
        <item name="android:textColorSecondary">#2C2C2C</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:screenOrientation">portrait</item>
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>



</resources>
